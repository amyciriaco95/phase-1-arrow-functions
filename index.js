//function expression

const divide = function(x, y) {
    return x / y;
}

//arrow function called square where the return value is the parameter (x) multiplied by itself//

const square = x => x * x;

//arrow function called add where the return value is the two parameters (x and y) are added together//

const add = (x, y) => x + y;

